<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Itineraries</title>
    <%- include('../partials/_nav-bar.ejs') %>
</head>
<body>
    <h1>Your Itineraries</h1>

    <% if (itineraries.length === 0) { %>
        <p>You have no itineraries yet. <a href="/itineraries/create">Create one now!</a></p>
    <% } else { %>
        <ul>
            <% itineraries.forEach(itinerary => { %>
                <li>
                    <a href="/itineraries/<%= itinerary._id %>">
                        <%= itinerary.title %>
                    </a> - <%= itinerary.duration %> days
                </li>
            <% }) %>
        </ul>
    <% } %>
</body>
</html>
